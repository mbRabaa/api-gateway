{
  "name": "api-gateway",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "pretest": "rm -f junit.xml",
    "test": "NODE_ENV=test jest --config=jest.config.js --coverage",
    "test:ci": "NODE_ENV=test jest --config=jest.config.js --ci --coverage",
    "start": "node server.js",
    "build": "echo 'Build step completed'"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "@babel/preset-env": "^7.23.2"
  }
}